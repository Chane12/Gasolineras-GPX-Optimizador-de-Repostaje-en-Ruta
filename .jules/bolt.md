## 2025-05-15 - Streamlit Popover for Destructive Actions
**Learning:** Using `st.popover` in Streamlit to wrap destructive action buttons (like deleting data) provides an intuitive confirmation dialog without requiring complex state management for custom modal components.
**Action:** Always wrap delete or "clear all" buttons in an `st.popover` to prevent accidental data loss and improve UX.
