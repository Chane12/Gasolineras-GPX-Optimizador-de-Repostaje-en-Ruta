## 2025-02-12 - Streamlit Button Tooltips
**Learning:** Adding `help` parameters to interactive elements like `st.button` and `st.link_button` is critical for screen reader accessibility and helps explain ambiguous actions.
**Action:** Always include a descriptive `help` string for interactive Streamlit elements, especially when they only contain icons or have non-obvious consequences.
